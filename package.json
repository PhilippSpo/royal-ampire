{
  "name": "royal-ampire",
  "version": "0.0.1",
  "private": true,
  "homepage": "https://philippspo.github.io/royal-ampire",
  "devDependencies": {
    "gh-pages": "0.12.0",
    "react-scripts": "0.8.3"
  },
  "dependencies": {
    "react": "15.4.1",
    "react-dom": "15.4.1",
    "react-modal": "1.6.4",
    "styled-components": "1.1.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "deploy": "npm run build && gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  }
}
